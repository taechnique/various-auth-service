server:
  port: 8932

spring:
  datasource:
    # docker run -it -d -p 3306:3894 -e MYSQL_ROOT_PASSWORD=1111 --name service-test-db mysql:5.7
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 1111
    url:jdbc:mysql://localhost:3894/mini_service
